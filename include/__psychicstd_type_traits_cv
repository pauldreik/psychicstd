#pragma once
#include <__psychicstd_remove_cv>

namespace std {

template <typename T>
using remove_const = __psychicstd_detail::__remove_const_impl<T>;
template <typename T> using remove_const_t = typename remove_const<T>::type;

template <typename T>
using remove_volatile = __psychicstd_detail::__remove_volatile_impl<T>;
template <typename T>
using remove_volatile_t = typename remove_volatile<T>::type;

template <typename T>
using remove_cv = __psychicstd_detail::__remove_cv_impl<T>;
template <typename T> using remove_cv_t = typename remove_cv<T>::type;

}
