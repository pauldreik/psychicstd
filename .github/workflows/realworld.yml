---
name: real-world project builds
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  rdfind:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Enable deb-src
        run: |
          sudo sed -i 's/^Types: deb$/Types: deb deb-src/' /etc/apt/sources.list.d/ubuntu.sources
          sudo apt-get update
      - name: Install Markdown formatter
        run: pipx install mdformat
      - name: Install rdfind build dependencies
        run: sudo apt-get build-dep -y rdfind
      - name: Build and benchmark rdfind with psychicstd
        run: >-
          scripts/benchmark_realworld.py --project rdfind
          --build-type both --reps 3
          --output use_on_realworld_projects/rdfind_speed_report.md
      - name: Upload speed report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: rdfind-speed-report
          path: use_on_realworld_projects/rdfind_speed_report.md
      # TODO: no project here is built with sanitizers yet. rdfind used to
      # have a "run with sanitizers" step, but it silently ignored its own
      # -a/-u flags and just rebuilt the same non-sanitized binary again.
  simdutf:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install Markdown formatter
        run: pipx install mdformat
      - name: Install ninja
        run: sudo apt-get install -y ninja-build
      - name: Build and test simdutf with psychicstd
        run: >-
          scripts/benchmark_realworld.py --project simdutf
          --build-type debug --reps 1
          --output use_on_realworld_projects/simdutf_speed_report.md
      - name: Upload speed report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: simdutf-speed-report
          path: use_on_realworld_projects/simdutf_speed_report.md
  cmake:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install ninja
        run: sudo apt-get install -y ninja-build
      - name: Build and test CMake with psychicstd
        run: >-
          scripts/benchmark_realworld.py --check-only
          --project cmake --build-type both
  googletest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install ninja
        run: sudo apt-get install -y ninja-build
      - name: Build and test GoogleTest with psychicstd
        run: >-
          scripts/benchmark_realworld.py --check-only
          --project googletest --build-type debug
  abseil:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - name: Install ninja
        run: sudo apt-get install -y ninja-build
      - name: Build and test Abseil base with psychicstd
        run: >-
          scripts/benchmark_realworld.py --check-only
          --project abseil --build-type debug
  boost-asio:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - name: Build and test Boost.Asio with psychicstd
        run: >-
          scripts/benchmark_realworld.py
          --check-only --project boost-asio
          --build-type debug
  boost-test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - name: Build and test Boost.Test with psychicstd
        run: >-
          scripts/benchmark_realworld.py
          --check-only --project boost-test
          --build-type debug
  bitcoin:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - name: Install Ninja
        run: sudo apt-get install -y ninja-build
      - name: Build Bitcoin Core crypto library with psychicstd
        run: >-
          scripts/benchmark_realworld.py
          --check-only --project bitcoin
          --build-type debug
  electron:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Build and test Electron core with psychicstd
        run: >-
          scripts/benchmark_realworld.py --check-only
          --project electron --build-type debug
  godot:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - name: Install SCons and pkg-config
        run: sudo apt-get install -y scons pkg-config
      - name: Build Godot core library with psychicstd
        run: >-
          scripts/benchmark_realworld.py
          --check-only --project godot
          --build-type debug
  llama-cpp:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - name: Install Ninja
        run: sudo apt-get install -y ninja-build
      - name: Build llama.cpp CPU libraries and model metadata
        run: >-
          scripts/benchmark_realworld.py
          --check-only --project llama.cpp
          --build-type debug
  nlohmann:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install Markdown formatter
        run: pipx install mdformat
      - name: Install ninja
        run: sudo apt-get install -y ninja-build
      - name: Build nlohmann JSON examples with psychicstd
        run: >-
          scripts/benchmark_realworld.py --project nlohmann
          --build-type debug --reps 1
          --output use_on_realworld_projects/nlohmann_speed_report.md
      - name: Upload speed report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: nlohmann-speed-report
          path: use_on_realworld_projects/nlohmann_speed_report.md
  opencv:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - name: Install Ninja
        run: sudo apt-get install -y ninja-build
      - name: Build and test OpenCV with psychicstd
        run: >-
          scripts/benchmark_realworld.py --check-only
          --project opencv --build-type release
  rapidjson:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - name: Install ninja
        run: sudo apt-get install -y ninja-build
      - name: Build and run RapidJSON examples with psychicstd
        run: >-
          scripts/benchmark_realworld.py --check-only
          --project rapidjson --build-type debug
  fmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install Markdown formatter
        run: pipx install mdformat
      - name: Install ninja
        run: sudo apt-get install -y ninja-build
      - name: Build and test fmt with psychicstd
        run: >-
          scripts/benchmark_realworld.py --project fmt
          --build-type debug --reps 1
          --output use_on_realworld_projects/fmt_speed_report.md
      - name: Upload speed report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: fmt-speed-report
          path: use_on_realworld_projects/fmt_speed_report.md
  flatbuffers:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install Markdown formatter
        run: pipx install mdformat
      - name: Install ninja
        run: sudo apt-get install -y ninja-build
      - name: Build and test FlatBuffers with psychicstd
        run: >-
          scripts/benchmark_realworld.py --project flatbuffers
          --build-type debug --reps 1
          --output use_on_realworld_projects/flatbuffers_speed_report.md
      - name: Upload speed report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: flatbuffers-speed-report
          path: use_on_realworld_projects/flatbuffers_speed_report.md
  catch2:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install Markdown formatter
        run: pipx install mdformat
      - name: Install ninja
        run: sudo apt-get install -y ninja-build
      - name: Build and test Catch2 with psychicstd
        run: >-
          scripts/benchmark_realworld.py --project catch2
          --build-type debug --reps 1
          --output use_on_realworld_projects/catch2_speed_report.md
      - name: Upload speed report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: catch2-speed-report
          path: use_on_realworld_projects/catch2_speed_report.md
  eigen:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install Markdown formatter
        run: pipx install mdformat
      - name: Build and benchmark Eigen tests with psychicstd
        run: >-
          scripts/benchmark_realworld.py --project eigen
          --build-type debug --reps 1
          --output use_on_realworld_projects/eigen_speed_report.md
      - name: Upload speed report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: eigen-speed-report
          path: use_on_realworld_projects/eigen_speed_report.md
  cppcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install Markdown formatter
        run: pipx install mdformat
      - name: Build and benchmark cppcheck with psychicstd
        run: >-
          scripts/benchmark_realworld.py --project cppcheck
          --build-type debug --reps 1
          --output use_on_realworld_projects/cppcheck_speed_report.md
      - name: Upload speed report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: cppcheck-speed-report
          path: use_on_realworld_projects/cppcheck_speed_report.md
  tesseract:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - name: Install Tesseract build dependencies
        run: sudo apt-get install -y libleptonica-dev ninja-build
      - name: Build and test Tesseract with psychicstd
        run: >-
          scripts/benchmark_realworld.py --check-only
          --project tesseract --build-type debug
  tensorflow:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - name: Build TensorFlow core platform libraries with psychicstd
        run: >-
          scripts/benchmark_realworld.py --check-only
          --project tensorflow --build-type debug
  ctre:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install ninja
        run: sudo apt-get install -y ninja-build
      - name: Build CTRE compile-time tests with psychicstd
        run: >-
          scripts/benchmark_realworld.py --check-only
          --project ctre --build-type debug
  react-native:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install ninja
        run: sudo apt-get install -y ninja-build
      - name: Build React Native host core with psychicstd
        run: >-
          scripts/benchmark_realworld.py --check-only
          --project react-native --build-type both
  inipp:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Build inipp with psychicstd
        run: scripts/benchmark_realworld.py --check-only --project inipp --build-type
          debug
  cxxopts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Build cxxopts with psychicstd
        run: scripts/benchmark_realworld.py --check-only --project cxxopts --build-type
          debug
  pocketfft:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Build pocketfft with psychicstd
        run: scripts/benchmark_realworld.py --check-only --project pocketfft --build-type
          debug
  pybind11:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install build dependencies
        run: sudo apt-get install -y ninja-build python3-dev
      - name: Build and test pybind11 with psychicstd
        run: >-
          scripts/benchmark_realworld.py --check-only
          --project pybind11 --build-type debug
  ssvstart:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Compile SSVStart with psychicstd
        run: >-
          scripts/benchmark_realworld.py --check-only
          --project ssvstart --build-type debug --compiler g++-14
  strong-type:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install Ninja
        run: sudo apt-get install -y ninja-build
      - name: Build and test strong_type with psychicstd
        run: >-
          scripts/benchmark_realworld.py --check-only
          --project strong-type --build-type debug
  trompeloeil:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install Ninja
        run: sudo apt-get install -y ninja-build
      - name: Build and test Trompeloeil with psychicstd
        run: >-
          scripts/benchmark_realworld.py --check-only
          --project trompeloeil --build-type debug
  zancle:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install Ninja
        run: sudo apt-get install -y ninja-build
      - name: Build Zancle Base/System with psychicstd
        run: >-
          scripts/benchmark_realworld.py --check-only
          --project zancle --build-type debug --compiler g++-14
